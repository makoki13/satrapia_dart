// https://github.com/tekartik/sembast.dart/blob/master/example/web/main.dart
//import 'package:sembast/sembast.dart';
//import 'package:sembast/sembast_memory.dart';

class DBlocal {
  /*
  Database db = null; //NO String, cambiar por objeto Pouch
  Store _store;
  String _nombreStore;

  DBlocal (this._nombreStore);

  init() async {
    this.db = await memoryFsDatabaseFactory.openDatabase("database.db");
    this._store = this.db.getStore(this._nombreStore);
  }

  getRegistro(String clave) async {
    Record registro = (await this._store.getRecord(clave));
    return registro;
  }

  putRegistro (Record reg) async {
    Record record = await db.putRecord(reg);
    return record;
  }
  **/
}
